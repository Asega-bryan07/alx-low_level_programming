#include "main.h"

/**
 * rev_string - function that reverses a string
 *
 * @s: string parameter
 * Return: 0
 */
void rev_string(char *s)
{
	int len = strlen(s);

	while (str--)
		putchar(*(s + str));
	putchar(10);
}
