#include "main.h"
/**
 * _abs - function that computes the absolute values for an integer
 *
 * @n: takes integer type input for a function
 * REturn: 0 success
 */
int _abs(int n)
{
	if (n < 0)

		n = (-1) * n;
	return (n);
}
